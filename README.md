# JARVIS(Jarbni visually)


## How to 
+ run local server
    ```sh
    php artisan serv
    ```
+ [install composer](https://getcomposer.org/Composer-Setup.exe)
+ install composer components 
    ```sh
    composer install
    ```
